\section{Corrosion Inspection for Railway Tracks}
\label{sec:opm}
\label{sec:applications}
A new generation of highly sensitive and precise magnetic sensors based on 
vapor cells has recently transitioned from development to the commercial 
stage. These devices, known as \textit{Optically Pumped Magnetometers} (OPMs), 
can detect extraordinarily weak magnetic fields, with sensitivities reaching 
the pico-tesla ($\mathrm{pT}/\sqrt{\mathrm{Hz}}$) or even femto-tesla 
($\mathrm{fT}/\sqrt{\mathrm{Hz}}$) scale. Crucially for practical applications, 
they maintain this extreme precision while operating within a wide dynamic range, 
allowing them to function reliably even in unshielded environments \cite{quspin, deans2021}.

The core operating principle of these sensors relies on using laser light to 
manipulate and measure the quantum spin states of an alkali metal gas 
(typically $^{87}\mathrm{Rb}$ or $^{133}\mathrm{Cs}$) enclosed in a glass 
cell \cite{tierney2019}. This physical process occurs in three consecutive 
stages, beginning with the preparation of the atomic state through optical 
pumping. During this first stage, a circularly polarized ($\sigma^+$) laser 
beam, called the pump beam, passes through the vapor and transfers its angular 
momentum to the alkali atoms, thereby aligning their spins in a specific 
direction. Concurrently, a constant magnetic field ($B_{\mathrm{DC}}$) is 
applied parallel to this laser to guide and stabilize the aligned atoms into a 
baseline energy state or Zeeman sublevel.

Once the atoms are prepared, the second stage involves their resonant 
excitation by means of an alternating radio-frequency magnetic field 
($B_{\mathrm{RF}}$) applied perpendicularly to the baseline field. When 
the frequency of this radio-frequency field matches the natural precession 
frequency of the atoms—known as the Larmor frequency ($\omega_L = \gamma B_{\mathrm{tot}}$, 
where $\gamma$ is the gyromagnetic ratio)—it drives a coherent transition in 
the atomic spins, forcing them to rotate synchronously in circles. 

Finally, the third stage consists of the optical interrogation and electronic 
demodulation of the system. To detect the atomic rotation without destroying 
the prepared quantum state, a second, linearly polarized laser beam, designated 
as the probe beam, is sent through the cell. As this probe light passes through 
the spinning atoms, its plane of polarization undergoes a periodic rotation due 
to the paramagnetic Faraday effect, oscillating at the exact frequency of the 
Larmor precession. A balanced polarimeter captures this oscillating light and 
converts it into an alternating electrical signal (AC). This signal is subsequently 
processed by a lock-in amplifier, which demodulates the AC signal using the 
radio-frequency field as a reference, yielding precise direct current (DC) outputs 
for both amplitude and phase. With this configuration, any shifts in the local magnetic 
field disrupt the atomic resonance, producing an immediate, proportional change 
in these DC voltage outputs.

The schematic layout of this radio-frequency OPM configuration, including the 
orthogonal arrangement of the laser beams and the magnetic fields, is illustrated 
in Figure~\ref{fig:opm_setup}.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.65\textwidth]{Images/deansopm.png}
    \caption{Schematic layout of a radio-frequency OPM. The alkali vapor is 
    spin-polarized along the static field ($B_{\mathrm{DC}}$) by a $\sigma^+$ 
    pump beam. An orthogonal AC magnetic field excites the Larmor precession, 
    which rotates the polarization plane of a perpendicular, linearly polarized 
    probe beam. This optical rotation is detected by a polarimeter and analyzed 
    using a lock-in amplifier. Adapted from \cite{deans2021}.}
    \label{fig:opm_setup}
\end{figure}

Several non-destructive inspection (NDI) experiments have successfully demonstrated 
the use of atomic magnetometers for Electromagnetic Induction Imaging (EMI) 
\cite{marmugi2020} and Magnetic Induction Tomography (MIT) \cite{wickenbrock2014}. 
Recent studies \cite{guilizzoni2016, deans2024} have characterized 
the penetration capabilities of these devices through structural barriers. 
It has been established that OPMs can achieve 
high-fidelity object imaging through thick conductive enclosures under unshielded, 
room-temperature conditions without requiring background subtraction, as shown 
in Figure~\ref{fig:opm_images}. Moreover, fully portable scanning heads equipped 
with active-compensation coils have been demonstrated, proving capable of operating 
within complex, real-world magnetic environments \cite{deans2021}.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.85\textwidth]{Images/wickenopm.png}
    \caption{OPM imaging of (a) a \SI{37}{\milli\meter}$\times$\SI{37}{\milli\meter} 
    square, (b) an isosceles triangle with one side of \SI{37}{\milli\meter} and 
    two sides of \SI{30}{\milli\meter}, and (c) a disk with a \SI{37}{\milli\meter} 
    diameter. Adapted from \cite{wickenbrock2014}.}
    \label{fig:opm_images}
\end{figure}

When microfabricated into advanced architectures such as LWVCs, OPMs can 
achieve ultra-compact footprints at the MEMS scale with minimal power 
requirements. By eliminating the bulky cryogenic cooling systems inherent 
to conventional Superconducting Quantum Interference Devices (SQUIDs), these 
miniaturized atomic sensors enable highly portable operations and scalable, 
multi-channel array configurations.

\subsection{Application Scenario: Railway Track Corrosion}
\label{sec:corrosion_scenario}

Building upon these non-destructive capabilities, this section introduces a
novel industrial application designed to leverage the extreme precision of
atomic magnetometry. The primary objective is to deploy an OPM integrated
with an LWVC sensor head to detect the onset and volumetric progression of
structural corrosion at the foot of operational railway tracks. To establish
a realistic baseline, the present work references a commercial QuSpin
Total-Field Magnetometer as the benchmark sensor configuration. This device
operates as a pulsed laser-pumped rubidium free-induction-decay
magnetometer, with its core technical characteristics outlined in
Table~\ref{tab:quspin_specs}. Of paramount importance to this real-world
application are its scalar sensitivity of $<3\,\mathrm{pT}/\sqrt{\mathrm{Hz}}$
and a dynamic range spanning from $1$ to $150\,\mathrm{\mu T}$, which
effectively covers the ambient magnetic environments encountered in
unshielded railway settings. Furthermore, the compact physical footprint of
the sensor head ($12\,\mathrm{g}$, $17.7 \times 19.8 \times 35.8\,%
\mathrm{mm}^3$) makes it highly compatible with portable, automated
scanning assemblies \cite{quspin}.

\begin{table}[h]
\centering
\caption{Key technical specifications of the QuSpin Zero-Field
    Magnetometer used as the reference OPM sensor in this work
    \cite{quspin}.}
\label{tab:quspin_specs}
\begin{tabular}{ll}
    \hline
    \textbf{Parameter} & \textbf{Value} \\
    \hline
    Sensor type & Pulsed laser-pumped Rb, free induction decay \\
    Scalar sensitivity & $<3\,\mathrm{pT}/\sqrt{\mathrm{Hz}}$ \\
    Vector sensitivity (optional) & $<0.1\,\mathrm{nT}/\sqrt{\mathrm{Hz}}$ \\
    Bandwidth & $500\,\mathrm{Hz}$ \\
    Data rate & $1000\,\mathrm{samples/s}$ \\
    Dynamic range & $1000$--$150{,}000\,\mathrm{nT}$ \\
    Deadzone & Axial only, $<\pm7^\circ$ cone about Earth's field \\
    Heading error & $<3\,\mathrm{nT}$ (uncompensated) \\
    Max.\ gradient field & $300\,\mathrm{nT/cm}$ \\
    Operating temperature & $-15$ to $+55\,^\circ\mathrm{C}$ \\
    Sensor head size & $17.7\times19.8\times35.8\,\mathrm{mm}$ \\
    Sensor head mass & $12\,\mathrm{g}$ \\
    Power consumption & $2.5\,\mathrm{W}$ ($3.5\,\mathrm{W}$ startup) \\
    \hline
    \end{tabular}
\end{table}

The tracking infrastructure under analysis adheres to a standard structural
rail profile.
These tracks are typically manufactured from high-strength pearlitic carbon
steel, characterized by a high electrical conductivity ($\sigma_{\mathrm{rail}}
\approx 10^6\,\mathrm{S/m}$) and a robust ferromagnetic relative
permeability ($\mu_{r,\mathrm{rail}} \approx 10^2$). When exposed to
atmospheric oxygen and persistent moisture, electrochemical oxidation
generates a localized degradation layer composed of iron oxides and
hydroxides—primarily $\mathrm{Fe_2O_3}$, $\mathrm{Fe_3O_4}$, and
$\mathrm{FeOOH}$—predominantly at the vulnerable rail foot region. This
resulting corrosion layer exhibits a severe material contrast compared to
the healthy steel substrate, as its electrical conductivity drops
dramatically ($\sigma_{\mathrm{corr}} \approx 10^{-4}\,\mathrm{S/m}$) and
its relative permeability falls to near unity ($\mu_{r,\mathrm{corr}}
\approx 1$), effectively rendering the domain paramagnetic.

The underlying detection mechanism exploits these electromagnetic contrasts
via eddy current induction. An excitation coil driven by an alternating
current produces a time-varying primary magnetic field, $B_1(t) = B_1
e^{i\omega t}$, which penetrates the conductive rail volume and induces
localized eddy currents ($\mathbf{J} = \sigma \mathbf{E}$) according to
Faraday's law. The amplitude of these induced currents decays exponentially
with depth as a function of the electromagnetic skin depth ($\delta$),
defined as:

\begin{equation}
    \delta = \sqrt{\frac{2}{\omega\mu\sigma}}
           = \sqrt{\frac{1}{\pi f \mu_0 \mu_r \sigma}}
    \label{eq:skin_depth}
\end{equation}

\noindent where $f$ represents the operational excitation frequency, $\mu_0$
is the vacuum permeability, $\mu_r$ is the relative permeability, and
$\sigma$ denotes the electrical conductivity of the respective domain.
Because the structural corrosion layer introduces a severe discontinuity in
both permeability and conductivity, the local eddy current distribution
becomes significantly perturbed. This disruption alters the secondary
magnetic field ($B_2$) generated by the currents, meaning the total magnetic
field ($B_{\mathrm{tot}} = B_1 + B_2$) carries a distinct electromagnetic
signature of the subsurface defect that can be resolved by the high-precision
OPM sensor.

\subsection{Finite-Element Simulation}
\label{sec:fem_simulation}
\textbf{FALTA POSAR LA COIL STRENGTH, FALTA SENSITIVITY PLOTS. TORNAR A
FER LES FOTOS 2D EN MES CALITAT}
To evaluate the mathematical feasibility and physical constraints of this
diagnostic concept prior to full experimental assembly, a comprehensive 2D
finite-element method (\gls{FEM}) simulation was executed within COMSOL
Multiphysics. Developed as a preliminary proof of concept, this numerical
framework forms part of an ongoing project at QSENSATO Srl.\ and reflects
a diagnostic system that is currently in its development phase. Operating via
the \textit{AC/DC Module}\footnote{It is, indeed, a long way 
to the top if you wanna rock and roll.} and utilizing the \textit{Magnetic Fields} physics
interface in a frequency-domain formulation, the framework compares a
baseline healthy rail against a defective track profile containing a
localized corrosion zone of variable depth at the rail foot.

\paragraph{Geometry, boundary conditions, and material properties.}
As depicted in Figure~\ref{fig:clearance_line}, the geometric domain
comprises the 2D cross-section of a standard rail profile (a) secured by
native fastening clamps (b). A localized corrosion domain (c) is embedded
directly at the structural foot of the rail. The AC excitation coils are
positioned a few centimeters above the upper rail surface (d) and are
modeled as two distinct rectangular conductor domains carrying a prescribed
external current density $\mathbf{J}_e$. The entire engineering assembly
is enclosed within a spacious air region (e) to eliminate unphysical
boundary constraints. The outermost boundaries of this air domain are
governed by a magnetic insulation condition ($\mathbf{n} \times \mathbf{A}
= 0$, where $\mathbf{A}$ represents the magnetic vector potential),
simulating an open domain at an infinite distance from the sources. Beneath
the track assembly, a structural granite foundation layer (f) is
implemented to simulate the electromagnetic feedback of real ballast stones.
Finally, to allow the field solutions to attenuate naturally without
artificial reflections, an Infinite Element Domain layer (g) encapsulates
the outermost perimeter of the setup. Within this model, the OPM is
represented by a spatial evaluation \textit{cutpoint}, subject to the
strict operational constraint that it must remain positioned above the
mechanical clearance line of the rail.

In terms of material property assignments, the bulk rail steel is designated
with an electrical conductivity of $\sigma_{\mathrm{rail}} = 6 \times
10^6\,\mathrm{S/m}$ and a relative magnetic permeability of
$\mu_{r,\mathrm{rail}} = 400$, which are highly representative of pearlitic
railway tracks. The corrosion defect (assumed to consist primarily of
$\mathrm{Fe_2O_3}$) is assigned a degraded conductivity of
$\sigma_{\mathrm{corr}} = 10^{-4}\,\mathrm{S/m}$ and a relative permeability
of $\mu_{r,\mathrm{corr}} = 5$. The surrounding air and infinite element
domains are treated as standard non-conductive, non-magnetic environments
($\sigma = 0\,\mathrm{S/m}$, $\mu_r = 1$), while the underlying granite
ballast is modeled as a lossy dielectric foundation with a conductivity of
$\sigma_{\mathrm{granite}} = 10^{-4}\,\mathrm{S/m}$ and a non-magnetic
relative permeability of $\mu_{r,\mathrm{granite}} = 1$.
\begin{figure}[htbp]
\centering
\includegraphics[width=0.65\textwidth]{Images/simulationsetup.png}
\caption{Geometric configuration of the 2D COMSOL simulation domain,
  illustrating the spatial distribution of (a) the rail profile, measuring
  approximately \SI{17}{\centi\meter} in width and \SI{20}{\centi\meter} in
  height, (b) the mechanical fastening clamps, (c) the localized foot
  corrosion defect, (d) the overhead AC excitation coils, (e) the bounding
  air region, (f) the underlying granite foundation ballast, and (g) the
  outermost Infinite Element Domain layer. The spatial evaluation cutpoint
  for the OPM sensor is constrained above the mechanical clearance line.}
\label{fig:clearance_line}
\end{figure}

\paragraph{Mesh strategy.}
An accurate evaluation of the secondary fields requires a specialized,
frequency-dependent dynamic meshing strategy capable of adjusting to the
changing skin depth ($\delta$) of the system. Because $\delta$ scales
inversely with the square root of the coil's excitation frequency, the
spatial resolution of the mesh must adapt dynamically to avoid severe
discretization errors. This multi-scale discretization strategy comprises a
general overview of the entire computational domain
(Figure~\ref{fig:mesh_composite}a), supplemented by detailed close-ups of
the upper section near the coils (Figure~\ref{fig:mesh_composite}b) and the
lower section near the track foot (Figure~\ref{fig:mesh_composite}c). To
accurately resolve the critical electromagnetic skin effect phenomena, the
entire bulk steel geometry is discretized using a uniform refinement with a
maximum element size tightly constrained to $\delta/3$.

Most notably, a continuous, dense boundary layer mesh is implemented to
completely encapsulate the rail profile, spanning all physical interfaces
including the rail--air, rail--clamp, rail--corrosion, and rail--foundation
boundaries. The high-resolution deployment of these five structured
inflation layers is highlighted in the close-up of the corrosion zone
(Figure~\ref{fig:mesh_composite}d), \textbf{EN ESTA PART SE 
HA FLIPAT UN POC, LA LAYER MESH ES SIMPLEMENT PER A MILLORAR
LA SIMULACIO ENTRE DOS MATERIALS} ensuring a precise discretization of
the subtle perturbations induced by the defect. Given that the skin depth
$\delta$ shifts from approximately \SI{6.5}{\milli\meter} at a low
frequency of \SI{10}{\hertz} down to \SI{0.46}{\milli\meter} at the
maximum study frequency of \SI{2}{\kilo\hertz} \textbf{COMPROVAR VALOR}, this adaptive parametric
mesh is generated automatically for each discrete frequency point.
Conversely, within the air and granite foundation domains where skin effect
attenuation is absent, a free triangular mesh with relaxed geometric
constraints is utilized, resulting in total mesh counts that scale
dynamically from thousands to tens of thousands of elements depending on
the excitation frequency.

\begin{figure}[htbp]
\centering
\begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{Images/generalmesh.png}
    \caption{General mesh layout}
    \label{fig:mesh_general}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{Images/topmesh.png}
    \caption{Refinement at the coils and rail top}
    \label{fig:mesh_top}
\end{subfigure}
\\ \vspace{1em}
\begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{Images/bottommesh.png}
    \caption{Refinement at clamps and corrosion zone}
    \label{fig:mesh_bottom}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{Images/corrosionmesh.png}
    \caption{Close-up of the boundary layer meshing}
    \label{fig:mesh_layer}
\end{subfigure}
\caption{Finite-element mesh discretization showing (a) the global domain
  overview, (b) localized refinement near the excitation coils, (c) detail
  of the rail foot assembly, and (d) high-resolution inflation layers
  tracking the corrosion boundaries.}
\label{fig:mesh_composite}
\end{figure}


\paragraph{Parametric sweep and engineering optimization.}

The simulation sweeps excitation frequencies from \SI{0.1}{\hertz} to
\SI{2}{\kilo\hertz}, and square corrosion zones with lateral size ranging
from zero (healthy baseline) to \SI{2.5}{\milli\meter} $\times$
\SI{2.5}{\milli\meter}. The primary engineering objective of this parametric
study is to navigate a critical trade-off between sensor saturation and
detection sensitivity. While the OPM must be positioned near the track to
resolve the weak magnetic anomalies produced by corrosion, the primary field
generated by the excitation coils is intensely strong and can easily
overwhelm the sensor. To mitigate this risk and prevent sensor blinding, the
OPM must be placed at an optimized standoff distance from the rail surface.
Simultaneously, the excitation frequency must be carefully tuned: higher
frequencies enhance the electromagnetic contrast within the shallow corrosion
layer but drastically reduce field penetration into the bulk steel. The
parametric sweep therefore aims to identify a specific operating window
where the coil parameters and the sensor position are simultaneously
compatible with both constraints.


\paragraph{Evaluation cutpoints and operational constraints.}

The simulation imposes two simultaneous operational constraints on any
candidate sensor position above the mechanical clearance line:

\begin{enumerate}
    \item \textbf{Dynamic range boundary:} The total field $B_{\mathrm{tot}}$
    must not exceed the QuSpin linear operating range of
    $\SI{150000}{\nano\tesla}$ to prevent sensor saturation.

    \item \textbf{Sensitivity boundary:} The differential field between
    healthy and corroded states must satisfy $\Delta B =
    |B_{\mathrm{healthy}} - B_{\mathrm{corroded}}| \geq \SI{3}{\pico\tesla}$,
    the scalar noise floor of the sensor.
\end{enumerate}

These two constraints define a feasibility window within which the
diagnostic system must operate. The following section presents the results
of the parametric study, building progressively from the qualitative field
distributions to the quantitative sensitivity analysis that ultimately
validates the chosen configuration.


%-----------------------------------------------------------------------
\subsection{Results and Analysis}
\label{sec:results_analysis}
%-----------------------------------------------------------------------

The results are presented in a stepwise manner. First, the spatial
distribution of the magnetic field is examined to provide physical intuition
about how the corrosion defect perturbs the flux. Second, the frequency
dependence of the skin depth is illustrated to motivate the choice of
excitation range. Finally, the differential signal $|\Delta B|$ is
evaluated as a function of both frequency and defect size, culminating in
the sensitivity plot that validates the feasibility of the proposed concept.


\paragraph{Field distribution: healthy versus corroded rail.}

As a first step, the spatial distribution of the total magnetic field was
computed for both the healthy and defective configurations. 
Figure~\ref{fig:field_comparison} provides a side-by-side comparison of
the generated fields under the (a) healthy and (b) corroded rail scenarios.
The structural degradation at the foot of the rail introduces a localized
perturbation in the magnetic flux lines: the low-permeability, low-conductivity
corrosion zone deflects and attenuates the flux that would otherwise close
through the intact steel, producing a measurable deviation in the external
field.

\begin{figure}[htbp]
\centering
\begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{Images/totalBhealthylowf1.png}
    \caption{Healthy rail profile}
    \label{fig:field_healthy}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{Images/totalBlowf1.png}
    \caption{Corroded rail profile}
    \label{fig:field_corroded}
\end{subfigure}
\caption{Finite-element simulation results comparing the induced magnetic
  field profiles across (a) the benchmark healthy track and (b) the track
  affected by structural foot corrosion.}
\label{fig:field_comparison}
\end{figure}

To examine these electromagnetic perturbations with higher geometric
precision, high-resolution close-ups were extracted at the two critical
regions of the rail profile. Figure~\ref{fig:closeup_top} and
Figure~\ref{fig:closeup_bottom} show the localized field variations at the
top head and the bottom foot, respectively, clarifying how the magnetic flux
redistributes around the structural boundaries of the defect.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{Images/totalBcoil.png}
\caption{Detailed close-up view of the simulated magnetic field
  distribution at the top head region of the corroded rail profile.}
\label{fig:closeup_top}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{Images/totalBclamps.png}
\caption{Detailed close-up view of the simulated magnetic field
  distribution at the bottom foot region of the corroded rail profile.}
\label{fig:closeup_bottom}
\end{figure}


\paragraph{Influence of excitation frequency on field penetration.}

Having established the qualitative nature of the corrosion-induced
perturbation, the role of the excitation frequency was examined next.
Figure~\ref{fig:skin_depth_frequencies} displays the global field panorama
for a fixed corrosion size of \SI{1}{\centi\meter} $\times$
\SI{1}{\centi\meter} under three representative frequencies: (a)
\SI{1}{\hertz}, (b) \SI{15}{\hertz}, and (c) \SI{100}{\hertz}. This
progression visually confirms the physical reduction of the electromagnetic
skin depth ($\delta$) as the frequency increases, illustrating how eddy
currents are progressively squeezed toward the outermost boundaries of the
steel substrate. At low frequencies, the field penetrates deeply into the
rail cross-section and reaches the foot where the corrosion is located; at
higher frequencies, field penetration becomes increasingly confined to the
surface layers. This skin effect sets the fundamental upper limit on the
usable excitation frequency for foot corrosion detection. The continuous
dynamic evolution of the skin depth across the full frequency spectrum is
available as supplementary material animation (GIF) in \cite{railgif}.

\begin{figure}[htbp]
\centering
\begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{Images/totalBlowf.png}
    \caption{\SI{1}{\hertz}}
    \label{fig:freq_1}
\end{subfigure}
\\ \vspace{1em}
\begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{Images/totalBmidf.png}
    \caption{\SI{15}{\hertz}}
    \label{fig:freq_15}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{Images/totalBhighf.png}
    \caption{\SI{100}{\hertz}}
    \label{fig:freq_100}
\end{subfigure}
\caption{Global magnetic field distribution for a fixed
  \SI{1}{\centi\meter}$\times$\SI{1}{\centi\meter} corrosion size,
  illustrating the progressive decay of the electromagnetic skin depth
  ($\delta$) across increasing excitation frequencies.}
\label{fig:skin_depth_frequencies}
\end{figure}


\paragraph{Differential field mapping.}

To map the diagnostic signature of the defect in the spatial domain,
the differential field magnitude $|\Delta B| = |B_{\mathrm{healthy}} -
B_{\mathrm{corroded}}|$ was evaluated across the simulation domain.
Figure~\ref{fig:db_50hz} illustrates this quantity at a fixed excitation
frequency of \SI{50}{\hertz}. The map reveals that the anomaly is most
pronounced in the immediate vicinity of the corrosion zone at the rail
foot, and that a non-negligible signal persists at the sensor location
above the rail head, motivating the use of a remote, non-contact
measurement strategy.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{Images/diffB.png}
\caption{Differential magnetic field magnitude $|\Delta B|$ evaluated at
  a fixed excitation frequency of \SI{50}{\hertz}, illustrating the
  spatial extent of the corrosion-induced anomaly.}
\label{fig:db_50hz}
\end{figure}


\paragraph{Optimal sensor and coil configuration.}

The preceding results establish that a measurable differential signal
exists, but they do not yet guarantee that the signal can be detected by
the QuSpin OPM under realistic operating conditions. Translating the
differential field maps into a concrete experimental configuration requires
satisfying the two operational constraints simultaneously: the total field
at the sensor must remain within the linear operating range of the sensor,
while the differential field must exceed its noise floor.

To this end, different coil strengths and sensor positions were
systematically explored within the parametric study. The outcome of this
optimization is illustrated in Figure~\ref{fig:sensor_position}: the OPM
is placed at a horizontal distance of \SI{7}{\centi\meter} from the
excitation coil, constrained above the mechanical clearance line.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.65\textwidth]{Images/placeholder.png}
\caption{Schematic representation of the selected sensor placement. The
  OPM is located at a horizontal standoff of \SI{7}{\centi\meter} from
  the excitation coil, above the mechanical clearance line of the rail
  profile.}
\label{fig:sensor_position}
\end{figure}

This position was selected in conjunction with a coil configuration of
\SI{0.5}{\ampere} and 200 turns, which produces a primary field strong
enough to induce a detectable perturbation at the rail foot while keeping
the total field at the sensor location within the QuSpin dynamic range.


\paragraph{Sensitivity analysis and feasibility demonstration.}

With the experimental configuration fixed, the final and most critical
result of the parametric study can be presented. Figure~\ref{fig:sensitivity_plot}
shows the differential field magnitude $|\Delta B(f)|$ as a function of
excitation frequency for corrosion defects of varying lateral size. This
sensitivity plot constitutes the central quantitative result of the
simulation campaign, as it directly addresses whether the proposed
diagnostic system is physically realizable.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{Images/placeholder.png}
\caption{Sensitivity plot of the differential field magnitude $|\Delta B(f)|$
  as a function of excitation frequency for corrosion defects of varying
  lateral size. The dashed black line indicates the QuSpin noise floor
  (\SI{3}{\pico\tesla}). The shaded grey region marks the sensor's linear
  operating range ($B_{\mathrm{tot}} \leq \SI{150000}{\nano\tesla}$). The
  dashed orange line reports the total field magnitude $B_{\mathrm{tot}}$
  at the sensor cutpoint, which remains just below the upper operating
  limit. Coil parameters: \SI{0.5}{\ampere}, 200 turns; sensor standoff:
  \SI{7}{\centi\meter}.}
\label{fig:sensitivity_plot}
\end{figure}

Three features of the plot are worth highlighting. First, the dashed
orange line, representing the total field $B_{\mathrm{tot}}$ at the sensor
location, lies just below the upper boundary of the shaded operating
region across the entire frequency range, confirming that the chosen
configuration avoids saturation without sacrificing proximity to the
primary source. Second, the differential signal produced by each corrosion
size decreases monotonically with frequency, consistently with the
progressive loss of field penetration illustrated in
Figure~\ref{fig:skin_depth_frequencies}. Third, and most importantly, the
plot demonstrates that defects of all studied sizes generate a signal that
exceeds the sensitivity threshold within a well-defined frequency range:
smaller corrosion zones (lateral dimension $\sim$\SI{2}{\milli\meter})
remain detectable at excitation frequencies up to approximately
\SI{100}{\hertz}, while larger defects (lateral dimension of the order of
\SI{10}{\milli\meter}) produce anomalies that exceed the noise floor at
frequencies beyond \SI{1}{\kilo\hertz}, where the enhanced electromagnetic
contrast at the defect boundary compensates for the reduced penetration
depth.

Taken together, these results confirm the numerical feasibility of the
proposed concept. The simulation demonstrates that, for the selected coil
and sensor configuration, corrosion defects spanning the full range of
studied sizes are, in principle, detectable with the QuSpin OPM, and that
the operating window identified here provides a viable starting point for
the forthcoming experimental validation.